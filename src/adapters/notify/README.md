# Notification adapters

Implementation wave W6.

The existing bot/channel should be the first adapter if practical. The domain depends only on `NotificationPort`, allowing future replacement without workflow changes.
