# Safe default: no final publish action is permitted.
FLERDVISION_MODE=disabled
TZ=Europe/Vienna
DATABASE_URL=file:./runtime/flerdvision.sqlite
RUNTIME_DIR=./runtime
BROWSER_PROFILE_DIR=./profiles
# Optional. Empty means auto-detect per platform: Linux /usr/bin/chromium (then chromium-browser,
# google-chrome), macOS /Applications/Google Chrome.app. Set it to pin an exact browser build.
CHROMIUM_EXECUTABLE_PATH=
EVIDENCE_DIR=./artifacts/evidence
# Comma-separated stable internal account IDs. Empty means none.
ALLOWED_PUBLISH_ACCOUNT_IDS=
# Separate hard gate; production code must require BOTH mode and this flag.
ALLOW_FINAL_PUBLISH=false

# W2 ingress. Root ID is deployment data, never source-controlled with customer identifiers.
GOOGLE_DRIVE_ROOT_FOLDER_ID=
GOOGLE_DRIVE_SOURCE_ID=current_google_drive
# Safe default: source completion has no external side effect until an adapter is deliberately selected.
SOURCE_DISPOSITION_ADAPTER=noop
SOURCE_DISPOSITION_WEBHOOK_URL=

# W6 operations / notifications. Keep Ops UI private; default deployment binds localhost.
FLERDVISION_NOTIFICATION_WEBHOOK_URL=
FLERDVISION_NOTIFICATION_WEBHOOK_TOKEN=
FLERDVISION_OPS_HOST=127.0.0.1
FLERDVISION_OPS_PORT=8787
FLERDVISION_OPS_USER=flerdvision
FLERDVISION_OPS_PASSWORD=
# Optional protected route to a browser-worker session launcher/noVNC gateway.
FLERDVISION_OPERATOR_SESSION_BASE_URL=

# W7/W8 AI repair provider. Social publishing itself remains browser-only.
AI_REPAIR_ENABLED=false
AI_REPAIR_MODE=disabled
AI_REPAIR_WRAPPER_COMMAND=
AI_REPAIR_TIMEOUT_MS=120000
# Provider secrets are deployment-only and must never be committed:
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# W8 private real-account acceptance. Keep false outside the one authorized test window.
FLERDVISION_E2E_ZERO_VIEWER_REQUIRED=true
