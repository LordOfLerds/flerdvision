# Operations

Deployment manifests/systemd/Docker Compose are implemented after the control-plane and browser-worker contracts are executable. The intended topology is documented in `docs/09-DEPLOYMENT.md`.
